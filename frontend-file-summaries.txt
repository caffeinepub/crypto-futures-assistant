{
  "frontend/public/service-worker.js": "PWA service worker that caches app-shell assets (cache-first) and now correctly bypasses interception for all external API domains (fapi.binance.com, api.binance.com, api.coingecko.com, ICP endpoints) when the device is online, only returning a 503 offline error when navigator.onLine is false.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all Binance API and backend interactions; `usePositionRisk` now calls `https://fapi.binance.com/fapi/v2/positionRisk` directly from the browser using `fetchSignedBinance` with HMAC-SHA256 signing, removing the Motoko canister proxy dependency entirely.",
  "frontend/src/lib/binance-auth.ts": "Binance credential storage/retrieval helpers and HMAC-SHA256 browser-side signing utilities; `fetchOpenPositions` is restored (no longer deprecated) and uses `/fapi/v2/positionRisk` directly, and `fetchSignedBinance` is the primary export for authenticated browser-side Binance Futures requests.",
  "frontend/src/pages/SearchTab.tsx": "Search tab with symbol analysis and Open Positions section; fixes the TypeScript `never` error by explicitly typing `positions` as `BinancePosition[]` via `positionsData ?? []`, eliminating all `!positions` null-checks in favor of direct `.length` checks on the always-defined array."
}